# GitHub Token 配置模板
# 使用方法：
# 1. 复制此文件为 .env (命令: copy .env.template .env)
# 2. 将下面的 your_token_here 替换为你的真实 GitHub Token
# 3. 运行脚本时会自动读取此文件中的环境变量

GITHUB_TOKEN=your_token_here

# 获取 Token 的方法：
# 1. 访问 https://github.com/settings/tokens
# 2. 点击 "Generate new token (classic)"
# 3. 勾选 repo 权限（完整仓库访问）
# 4. 生成后复制 Token（只显示一次！）
# 5. 将 Token 粘贴到下面

# ⚠️ 安全提醒：
# - 不要将真实的 .env 文件提交到 Git
# - .gitignore 已配置忽略 .env 文件
# - 如果 Token 泄露，立即在 GitHub 上撤销
