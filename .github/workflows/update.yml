name: 每日更新基金数据

on:
  # 每天北京时间 0:01、8:00、19:00、21:00、23:00 执行
  # 0:01  获取前一工作日已确认净值
  # 8:00  获取早盘前净值更新
  # 19:00 获取收盘后净值（可标注"已根据今日净值更新"）
  # 21:00 获取晚间净值更新
  # 23:00 获取当日最后一次净值更新
  schedule:
    - cron: '1 16 * * *'
    - cron: '0 0 * * *'
    - cron: '0 11 * * *'
    - cron: '0 13 * * *'
    - cron: '0 15 * * *'
  # 支持手动触发
  workflow_dispatch:
  # 推送到 main 分支时也触发
  push:
    branches: [main]

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # ===== 合并为单步：更新数据并部署 =====
  update-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 安装依赖
        run: pip install -r requirements.txt

      - name: 运行数据抓取脚本
        run: python fetch_fund_data.py

      - name: 提交更新数据
        run: |
          set -e
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          git diff --cached --quiet || git commit -m "自动更新基金数据+持仓快照 $(date +%Y-%m-%d)"
          # 先拉取远程最新更改，避免推送冲突
          git pull --rebase origin main
          git push origin main

      - name: 设置 Pages
        uses: actions/configure-pages@v5

      - name: 准备部署文件
        run: |
          mkdir -p site/data
          cp index.html site/
          # 只部署必要的数据文件（不包含原始交易记录）
          cp data/funds_data.json site/data/
          cp data/holdings_snapshot.json site/data/
          touch site/.nojekyll

      - name: 上传 Pages 产物
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'site'

      - name: 部署到 Pages
        id: deployment
        uses: actions/deploy-pages@v4
