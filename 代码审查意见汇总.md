# 基金追踪器 · 前端代码审查意见汇总（意见 1–24）

> 汇总更新于 **2026-07-17**。原始意见来自用户提交的代码审查截图（针对 `index.html` 单文件应用，约 6300 行）。
>
> 状态图例：✅ 已修复 ｜ 🟡 已评估·暂缓（未实施）｜ ⚪ 待评估
> 关联提交：共 6 个 —— `0202a15`、`ccdb5ca`、`15064cb`、`3eac338`、`367a7cb`、`9ae000c`

---

## 一、早期问题集（意见 1–5）—— 已全部修复

| 编号 | 类别 | 问题描述 | 修复方案 | 状态 | Commit |
|------|------|----------|----------|------|--------|
| 1 | 规范 | `<style>`/`<script>` 标签内残留 HTML 注释 `<!-- -->` | 改为 CSS/JS 注释 `/* */`（仅改标签内的非法注释） | ✅ | `0202a15` |
| 2 | 时序 | `window.onload` 与 `DOMContentLoaded` 时序冲突，事件绑定可能早于 DOM 初始化 | 抽出 `initApp()`，在 `DOMContentLoaded` 监听器开头调用 | ✅ | `0202a15` |
| 3 | 时区 | `getBeijingNow()` 用 `new Date(toLocaleString('en-US',{timeZone}))` 字符串回灌，跨浏览器实现相关、易错 | 改用 `Intl.DateTimeFormat().formatToParts()` 提取字段构造（保留 DST 安全语义，不退回手动偏移） | ✅ | `ccdb5ca` |
| 4 | XSS | `innerHTML` 插入未转义用户数据（`showConfirmDialog` 的 fundName/code、`renderRecordsList` 金额字段） | 关键字段包裹 `escapeHtml()` | ✅ | `15064cb` |
| 5 | XSS | JSONP 请求可被注入；回调名可预测（`fundSearchCbN`） | `^https://` 协议白名单 + 随机回调名 `fundSearchCb_`；移除死状态 `state.search.jsonpCallbackId` | ✅ | `15064cb` |

---

## 二、代码审查批注集（意见 7–24）

### ✅ 已修复（3 个批次，共 8 项）

**第 1 批 · 可靠性（`3eac338`，意见 19/20/21/23）**

| 编号 | 问题描述 | 修复方案 | 状态 |
|------|----------|----------|------|
| 19 | Toast 堆积：多个提示重叠 | `showMessage` 限流最多 3 个，移除最旧后 `forEach` 重排剩余 `top`（20/90/160px） | ✅ |
| 20 | `localStorage` 裸调用无 try-catch（隐私模式/配额满即崩） | 新增 `safeGetItem`/`safeSetItem` 封装，替换全部 12 处裸调用 | ✅ |
| 21 | `fmtNum` 对字符串数字（如 `"123"`）返回 `0.00` | 改用 `Number(v)` + `isFinite(n)?n:0` | ✅ |
| 23 | 轮询（`setInterval`）未在页面关闭时清理 | 新增 `stopPolling()` + `beforeunload` 注册清理 | ✅ |

**第 2 批 · A11y 与健壮性（`367a7cb`，意见 16/18/22）**

| 编号 | 问题描述 | 修复方案 | 状态 |
|------|----------|----------|------|
| 16 | 管理弹窗缺 ARIA 语义（屏幕阅读器不可识别） | `manageModal` 加 `role="dialog" aria-modal="true" aria-labelledby="manageModalTitle"`，标题 `<h2>` 加对应 `id` | ✅ |
| 18 | `fund-metrics` 表头用 `<td>` 而非 `<th>`，语义缺失且 `table-layout:fixed` 下列宽错位 | 表头改 `<th scope="col">` 并扩展 CSS 选择器（`.fund-metrics th`、`.metric-labels th`、`:nth-child` 含 `th`、响应式） | ✅ |
| 22 | `importFromFile` 解析后直接赋值，结构非法会导致后续崩溃 | 新增 `isValidPurchaseRecords()` 轻量结构校验，非法则提示并 `return` | ✅ |

**第 3 批 · Chart.js 实例复用（`9ae000c`，意见 7）**

| 编号 | 问题描述 | 修复方案 | 状态 |
|------|----------|----------|------|
| 7 | Chart.js 实例重建：对比图/单基金图每次重渲染都 `destroy()`+`new Chart()`，造成不必要的实例销毁重建与潜在内存压力 | `renderFundChart`/`renderCompareChart` 改为增量更新：实例存在时仅替换 `data.labels`/`data.datasets`（及 `tooltip.callbacks` 闭包），调用 `update()` 复用；无数据时清理旧实例。单基金图在周期切换时复用同一实例并刷新 tooltip 回调，避免闭包引用过期数据；对比图图例在每次渲染（含增量更新）都刷新 | ✅ |

### 🟡 已评估 · 暂缓（未实施，共 10 项）

| 编号 | 主题 | 评估结论 / 暂缓理由 |
|------|------|---------------------|
| 8 | 字符串拼接渲染 | 部分 DOM 用字符串拼接构造。评估：已统一 `escapeHtml` 转义，安全无碍；改模板引擎属过度抽象（Simplicity First） |
| 9 | `escapeHtml` 性能 | 评估：每行调用开销可忽略，未构成瓶颈，无需缓存/批量优化 |
| 10 | 搜索防抖 | 评估：仅 11 只基金，输入即筛开销极低，防抖收益低 |
| 11 | 架构重构（模块化拆分单文件） | 评估：将单文件拆分为 ES 模块。暂缓：违背"单文件纯静态、零构建"设计初衷，引入构建步骤得不偿失 |
| 12 | CSS 命名规范 | 评估：当前类名较随意但功能无影响。暂缓：全量重命名风险高、收益低 |
| 13 | A11y（其他交互） | 评估：部分交互缺键盘可达性。暂缓：核心流程已可操作，逐步完善 |
| 14 | 架构/CSS 重构（同 11/12 批） | 同架构重构批，暂缓 |
| 15 | A11y 批（同 13） | 同 A11y 批，暂缓 |
| 17 | A11y/健壮性批（非 16/18/22 范围） | 评估：未纳入本次修复；状态同该批其余项，暂缓 |
| 24 | CSS 特异性冲突 | 评估：个别样式靠高特异性/`!important` 覆盖。暂缓：需重排选择器权重，风险高收益低 |

---

## 三、统计

- **总意见数**：24（意见 1–5 ＋ 意见 7–24；原批注序列未含意见 6）
- ✅ **已修复**：12 项（意见 1–5 ＋ 19/20/21/23 ＋ 16/18/22）
- 🟡 **已评估·暂缓**：12 项（意见 7–15、17、24）
- **关联提交**：5 个（`0202a15`、`ccdb5ca`、`15064cb`、`3eac338`、`367a7cb`），均已推送 GitHub Pages 并验证

---

## 四、备注

- 意见编号来自用户截图。意见 1–5、16、18、19、20、21、22、23 的描述与修复方案有详细对话/日志佐证；意见 7–15、17、24 以当时评估的**主题关键词**重建，精确行号/原措辞以当时对话为准。
- 暂缓项均经评估，主要依据：① 单文件纯静态架构的设计约束；② Simplicity First 原则（避免无收益抽象）；③ 低风险、当前无功能性缺陷。
- 后续如需实施任一暂缓项，建议单独评估影响面后再动手，避免牵一发而动全身。
- 另：Python 端独立审查见 `BUG_REVIEW_REPORT.md`（2026-07-07，含 `offset=2` HIGH 问题），不属本次意见 1–24 范围。
